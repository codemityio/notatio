# `graphviz`

## Table of contents

- [Summary](#summary)
- [Manual](#manual)
- [Usage](#usage)

## Summary

A tool to convert `dot`/`gv` files to `svg`/`png` images.

## Manual

``` bash
$ notatio graphviz --help
NAME:
   notatio graphviz

USAGE:
   notatio graphviz [command options]

DESCRIPTION:
   A tool to convert dot/gv files to svg/png images.

OPTIONS:
   --input-path value     input path, either a file to be converted or a directory to be scanned (default: ".")
   --output-format value  output format (svg or png) (default: "svg")
   --recursive            enable recursive directories scan (default: false)
   --help, -h             show help
```

## Usage

Coming soon…
